version: '3.3'

services:

  app:
    build: .
    ports:
      - 8080:8080

#  nginx:
#    image: nginx:1.19.3
#    ports:
#      - 8000:8000
#    volumes:
#      - ./nginx.conf:/etc/nginx/conf.d/default.conf
#    restart: always
